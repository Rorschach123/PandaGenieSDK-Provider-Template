<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="ai.rorsch.pandagenie.sdk.ROLE"
            android:value="provider" />
        <meta-data
            android:name="ai.rorsch.pandagenie.sdk.NAME"
            android:value="@string/app_name" />
        <meta-data
            android:name="ai.rorsch.pandagenie.sdk.VERSION"
            android:value="1.0.0" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".DemoCapabilityService"
            android:exported="true">
            <intent-filter>
                <action android:name="ai.rorsch.pandagenie.sdk.action.CAPABILITY_SERVICE" />
            </intent-filter>
        </service>

        <provider
            android:name=".DemoContentProvider"
            android:authorities="ai.rorsch.pandagenie.sdkdemo.provider.samples"
            android:exported="true" />

        <receiver
            android:name=".DemoBroadcastReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="ai.rorsch.pandagenie.sdkdemo.provider.DEMO_PING" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
